import React, { useEffect, useState } from "react";
import { CheckCircle2, ChevronLeft, ChevronRight, Clock3, Code2, Eye, FileText, Flag, Moon, Save, Send, ShieldCheck, Sun, X } from "lucide-react";
import useWorkspaceTheme from "./useWorkspaceTheme";

const questions = [
  { id: 1, type: "Trắc nghiệm", title: "Thuật toán nào tìm đường đi ngắn nhất từ một đỉnh?", state: "done" },
  { id: 2, type: "Điền đáp án", title: "Độ phức tạp của binary search là bao nhiêu?", state: "current" },
  { id: 3, type: "Lập trình", title: "Tìm số phần tử khác nhau trong mảng", state: "review" }
];

const starterCode = "#include <bits/stdc++.h>\\nusing namespace std;\\n\\nint main() {\\n  // Viết lời giải tại đây\\n  return 0;\\n}";

function StatusBar({ guideViewed, focusEvents }) {
  return <div className="flex flex-wrap items-center gap-x-4 gap-y-1 border-b border-slate-100 bg-[#F8FAFB] px-4 py-2 text-[10px] text-slate-500">
    <span className="inline-flex items-center gap-1.5 font-bold text-[#126F91]"><FileText className="h-3.5 w-3.5" />Đề chuẩn PDF</span>
    <span className="inline-flex items-center gap-1.5"><Eye className="h-3.5 w-3.5 text-amber-600" />Hướng dẫn: {guideViewed ? "Đã mở" : "Chưa mở"}</span>
    <span className="inline-flex items-center gap-1.5"><ShieldCheck className="h-3.5 w-3.5 text-emerald-600" />{focusEvents > 0 ? focusEvents + " tín hiệu rời màn hình" : "Đang theo dõi phiên làm bài"}</span>
  </div>;
}

export default function AssessmentModal({ isOpen, onClose }) {
  const [active, setActive] = useState(1);
  const [answer, setAnswer] = useState("");
  const [code, setCode] = useState(starterCode);
  const [submitted, setSubmitted] = useState(false);
  const [guideViewed, setGuideViewed] = useState(false);
  const [focusEvents, setFocusEvents] = useState(0);
  const [theme, toggleTheme] = useWorkspaceTheme(isOpen);
  const current = questions.find((question) => question.id === active) || questions[0];

  useEffect(() => {
    if (!isOpen) return undefined;
    const handleVisibilityChange = () => {
      if (document.hidden) setFocusEvents((count) => count + 1);
    };
    document.addEventListener("visibilitychange", handleVisibilityChange);
    return () => document.removeEventListener("visibilitychange", handleVisibilityChange);
  }, [isOpen]);

  if (!isOpen) return null;

  if (submitted) return <div className="fixed inset-0 z-[70] flex items-center justify-center bg-slate-950/55 p-3 sm:p-6"><div className="w-full max-w-3xl overflow-hidden rounded-2xl bg-white shadow-2xl"><div className="bg-gradient-to-r from-[#0B4F86] via-[#166A9B] to-[#2B93BA] p-5 text-white sm:p-6"><p className="text-[10px] font-bold uppercase tracking-wider text-sky-100">Đề đã nộp</p><h2 className="mt-1 text-xl font-bold">Kết quả tạm tính: 72 / 100</h2><p className="mt-2 text-xs leading-relaxed text-sky-100">Câu lập trình đang chấm. Điểm cuối cùng sẽ được cập nhật sau khi có verdict.</p></div><div className="grid divide-y divide-slate-100 p-3 sm:grid-cols-3 sm:divide-x sm:divide-y-0"><div className="p-3"><p className="text-[10px] font-bold uppercase text-slate-400">Đã trả lời</p><p className="mt-1 text-xl font-black text-emerald-600">2 / 3</p></div><div className="p-3"><p className="text-[10px] font-bold uppercase text-slate-400">Đang chấm</p><p className="mt-1 text-xl font-black text-amber-500">1 câu</p></div><div className="p-3"><p className="text-[10px] font-bold uppercase text-slate-400">Tín hiệu phiên</p><p className="mt-1 text-xl font-black text-sky-700">{focusEvents}</p></div></div><div className="flex flex-wrap items-center justify-between gap-3 border-t border-slate-100 p-4"><p className="text-[11px] text-slate-500">Hướng dẫn: {guideViewed ? "đã mở" : "chưa mở"} · dữ liệu được lưu theo phiên.</p><button type="button" onClick={onClose} className="rounded-xl bg-[#126F91] px-4 py-2.5 text-[11px] font-bold text-white">Xem lịch sử nộp</button></div></div></div>;

  return <div className="fixed inset-0 z-[70] flex items-center justify-center bg-slate-950/55 p-0 sm:p-5"><div className="flex h-full w-full max-w-6xl flex-col overflow-hidden bg-white shadow-2xl sm:h-[min(760px,calc(100vh-40px))] sm:rounded-2xl">
    <header className="flex shrink-0 items-center gap-3 border-b border-slate-100 px-4 py-3 sm:px-6"><button type="button" onClick={onClose} aria-label="Đóng đề thi" className="rounded-xl p-2 hover:bg-slate-100"><X className="h-5 w-5 text-slate-500" /></button><div className="min-w-0 flex-1"><p className="text-[10px] font-bold uppercase tracking-wider text-blue-600">Đề luyện tuần 4 · đã lưu tự động</p><h2 className="truncate text-sm font-bold text-slate-800 sm:text-base">Kiểm tra chuyên đề Cấu trúc dữ liệu</h2></div><button type="button" onClick={toggleTheme} className="grid h-8 w-8 shrink-0 place-items-center rounded-xl border border-sky-100 bg-white text-slate-600 transition-colors hover:bg-sky-50 hover:text-[#126F91] focus:outline-none focus-visible:ring-2 focus-visible:ring-sky-400" aria-label={theme === "dark" ? "Chuyển phòng làm bài sang giao diện sáng" : "Chuyển phòng làm bài sang giao diện tối"} aria-pressed={theme === "dark"} title={theme === "dark" ? "Giao diện sáng" : "Giao diện tối"}>{theme === "dark" ? <Sun className="h-4 w-4" /> : <Moon className="h-4 w-4" />}</button><div className="hidden items-center gap-2 rounded-xl bg-amber-50 px-3 py-2 text-amber-700 sm:flex"><Clock3 className="h-4 w-4" /><span className="text-xs font-black">01:27:42</span></div><button type="button" className="hidden items-center gap-1.5 rounded-xl px-3 py-2 text-xs font-bold text-slate-600 hover:bg-slate-50 sm:flex"><Save className="h-4 w-4" />Đã lưu</button><button type="button" onClick={() => setSubmitted(true)} className="flex items-center gap-1.5 rounded-xl bg-[#126F91] px-3 py-2 text-xs font-bold text-white"><Send className="h-4 w-4" />Nộp bài</button></header>
    <StatusBar guideViewed={guideViewed} focusEvents={focusEvents} />
    <div className="grid min-h-0 flex-1 md:grid-cols-[220px_minmax(0,1fr)]">
      <aside className="overflow-y-auto border-r border-slate-100 bg-slate-50 p-3"><p className="px-2 text-[10px] font-bold uppercase tracking-wider text-slate-400">Điều hướng câu hỏi</p><div className="mt-3 space-y-2">{questions.map((question) => <button type="button" key={question.id} onClick={() => setActive(question.id)} className={"flex w-full items-center gap-2 rounded-xl p-2.5 text-left transition-colors " + (question.id === active ? "bg-[#126F91] text-white shadow-sm" : "border border-slate-100 bg-white text-slate-700 hover:border-sky-200")}><span className={"grid h-6 w-6 place-items-center rounded-lg text-[11px] font-black " + (question.id === active ? "bg-white/20" : question.state === "done" ? "bg-emerald-100 text-emerald-700" : question.state === "review" ? "bg-amber-100 text-amber-700" : "bg-slate-100")}>{question.state === "done" ? "✓" : question.id}</span><span className="min-w-0"><span className="block text-[10px] font-bold opacity-75">{question.type}</span><span className="block truncate text-[11px] font-bold">Câu {question.id}</span></span>{question.state === "review" && <Flag className="ml-auto h-3.5 w-3.5" />}</button>)}</div><div className="mt-4 rounded-xl border border-sky-100 bg-sky-50 p-3"><p className="text-[10px] font-bold text-sky-700">Trạng thái</p><p className="mt-1 text-[10px] text-slate-500">✓ Đã trả lời · ⚑ Đánh dấu xem lại</p></div></aside>
      <main className="min-w-0 overflow-y-auto p-4 sm:p-6"><div className="mx-auto max-w-3xl"><div className="mb-4 overflow-hidden rounded-xl border border-sky-100 bg-white shadow-sm"><div className="flex items-center justify-between gap-2 border-b border-sky-100 bg-sky-50/60 px-3 py-2"><span className="inline-flex items-center gap-1.5 text-[10px] font-bold uppercase tracking-[0.08em] text-sky-700"><FileText className="h-3.5 w-3.5" />PDF đề thi</span><button type="button" onClick={() => setGuideViewed(true)} className="inline-flex items-center gap-1 text-[10px] font-bold text-amber-700 hover:underline"><Eye className="h-3.5 w-3.5" />Xem hướng dẫn</button></div><div className="px-3 py-2 text-[11px] leading-relaxed text-slate-500">Đề thi và hướng dẫn được quản lý theo phiên bản PDF. Mã câu hiện tại: {current.id} · {guideViewed ? "Đã ghi nhận mở hướng dẫn" : "Chưa mở hướng dẫn"}.</div></div><div className="flex items-center gap-2"><span className="rounded-full bg-blue-50 px-2.5 py-1 text-[10px] font-bold text-blue-600">Câu {current.id} · {current.type}</span><button type="button" className="ml-auto flex items-center gap-1 text-[10px] font-bold text-amber-600"><Flag className="h-3.5 w-3.5" />Đánh dấu xem lại</button></div><h3 className="mt-4 text-base font-bold leading-snug text-slate-800 sm:text-lg">{current.title}</h3>{active === 1 && <div className="mt-5 grid gap-2">{["Dijkstra", "Kruskal", "Quick sort", "DFS"].map((option, index) => <label key={option} className={"flex cursor-pointer items-center gap-3 rounded-xl border p-3 text-xs font-semibold " + (index === 0 ? "border-sky-300 bg-sky-50 text-sky-700" : "border-slate-200 hover:border-sky-200")}><input type="radio" name="q1" defaultChecked={index === 0} />{option}</label>)}</div>}{active === 2 && <div className="mt-5"><label className="block text-xs font-bold text-slate-700">Đáp án của bạn <span className="text-rose-500">*</span></label><input value={answer} onChange={(event) => setAnswer(event.target.value)} placeholder="Ví dụ: O(log n)" className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-3 text-sm outline-none focus:border-sky-400 focus:ring-2 focus:ring-sky-100" /><p className="mt-2 text-[10px] text-slate-500">Nhập theo đúng định dạng. Hệ thống không phân biệt khoảng trắng thừa.</p></div>}{active === 3 && <div className="mt-5 overflow-hidden rounded-2xl border border-sky-100 bg-sky-50/40"><div className="flex justify-between border-b border-sky-100 bg-white px-4 py-2 text-[10px] text-slate-500"><span className="inline-flex items-center gap-1.5 font-bold text-[#126F91]"><Code2 className="h-3.5 w-3.5" />C++17</span><span>Nháp được lưu tự động</span></div><textarea value={code} onChange={(event) => setCode(event.target.value)} spellCheck={false} className="h-56 w-full resize-none bg-white p-4 font-mono text-xs leading-6 text-slate-700 outline-none" /></div>}<div className="mt-8 flex items-center justify-between"><button type="button" onClick={() => setActive(Math.max(1, active - 1))} className="flex items-center gap-1 rounded-xl px-3 py-2 text-xs font-bold text-slate-600 hover:bg-slate-100"><ChevronLeft className="h-4 w-4" />Câu trước</button><button type="button" onClick={() => setActive(Math.min(3, active + 1))} className="flex items-center gap-1 rounded-xl bg-[#126F91] px-3 py-2 text-xs font-bold text-white">Câu tiếp<ChevronRight className="h-4 w-4" /></button></div></div></main>
    </div>
  </div></div>;
}
